{% extends 'base.html.twig' %}

{% block title %}Role index{% endblock %}

{% block body %}
    <div class="bg-blueclear my-5 p-5">
        <h1>Role index</h1>
    </div>

    <table class="table table-responsive ">
        <thead>
            <tr class="text-center bg-blue text-light">
                <th>Id</th>
                <th>Libelle</th>
                <th>Code_role</th>
                <th >Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for role in roles %}
            <tr>
                <td class="text-center">{{ role.id }}</td>
                <td class="text-center">{{ role.libelle }}</td>
                <td class="text-center">{{ role.codeRole }}</td>
                <td class="text-center">
                    <a class="btn btn-md btn-green" href="{{ path('app_role_show', {'id': role.id}) }}">Afficher</a>
                    <a  class="btn btn-md btn-blue" href="{{ path('app_role_edit', {'id': role.id}) }}">Modifier</a>
                    <a  class="btn btn-md btn-red" href="{{ path('app_role_delete', {'id': role.id}) }}">Supprimer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="text-center">
        <a class="btn btn-md btn-blue ms-5 my-5" href="{{ path('app_role_new') }}">Créer un nouveau rôle</a>
    </div>
{% endblock %}
