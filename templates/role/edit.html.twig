{% extends 'base.html.twig' %}

{% block title %}Administrateur/ Modifier le Rôle{% endblock %}

{% block body %}
    <div class="bg-blueclear p-5 m-5">
        <h1>Modifier le rôle</h1>
    </div>
    <div class="m-auto w-50 border border-1 border-color shadow bg-clear">
        {{ include('role/_form.html.twig', {'button_label': 'Mettre à jour '}) }}
    </div>
    <div class="d-flex justify-content-around m-5">
        <a class="btn btn-md btn-blue" href="{{ path('app_role_index') }}">Revenir à la liste</a>
    
        {{ include('role/_delete_form.html.twig') }}
    </div>
   
{% endblock %}
