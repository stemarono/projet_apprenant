{% extends 'base.html.twig' %}

{% block title %}Administateur/ Fiche Utilisateur{% endblock %}

{% block body %}
    <div class=" bg-blueclear my-5 p-5">
        <h1>Fiche utilisateur</h1>
    </div>
    <table class="table table-responsive my-5">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Identifiant</th>
                <td>{{ user.identifiant }}</td>
            </tr>
             <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
            
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            
                <th>Mot de passe</th>
                <td>{{ user.password }}</td>
            </tr>
           
        </tbody>
    </table>

    <div class="d-flex justify-content-around my-5">
        <a  class="btn btn-md btn-green"href="{{ path('app_user_index') }}">Revenir à la liste</a>

        <a class="btn btn-md btn-blue" href="{{ path('app_user_edit', {'id': user.id}) }}">Modifier</a>

        {{ include('user/_delete_form.html.twig') }}
    </div>
{% endblock %}
