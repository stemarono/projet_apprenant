{% extends 'base.html.twig' %}

{% block title %}Administrateur/modifier un utilisateur{% endblock %}

{% block body %}

    {# titre #}
	<div class="container p-5 mt-5 bg-blueclear">
		<h1 class="pt-2 pb-5 text-clear">Modifier l'utilisateur</h1>
	</div>

	{# Bouton retour #}
	<div class="d-md-flex p-5 justify-content-md-start d-print-none">
		<button class="btn bg-blue mx-5 align-start">
			<a class="text-clear" href="javascript:history.back()">Retour</a>
		</button>
	</div>

	{# formulaire #}
	<div class="container w-50 mb-5">
		{{ include('user/_form.html.twig') }}
	</div>

{% endblock %}
