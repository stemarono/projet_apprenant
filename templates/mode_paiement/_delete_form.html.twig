<form method="post" action="{{ path('app_mode_paiement_delete', {'id': mode_paiement.id}) }}" onsubmit="return confirm('Êtes-vous sûr.e de vouloir supprimer cette mode_paiement ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mode_paiement.id) }}">
    <button class="btn bg-red text-clear m-5 align-end">Supprimer</button>
</form>

