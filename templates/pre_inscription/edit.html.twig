{% extends 'base.html.twig' %}

{% block title %}Modifier votre pré-inscription{% endblock %}

{% block body %}
    <h1>Modifiez votre pré-inscription </h1>
       
        <div class="m-auto w-50">
            {{ include('pre_inscription/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
        <div class="d-flex justify-content-between my-5">
            <a  class="btn btn-lg btn-blue "href="{{ path('app_espace_personnel_show') }}">Revenir à votre espace personnel</a>

            {{ include('pre_inscription/_delete_form.html.twig') }}
        </div>
{% endblock %}
