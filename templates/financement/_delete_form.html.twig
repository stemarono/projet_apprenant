<form method="post" action="{{ path('app_financement_delete', {'id': financement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ financement.id) }}">
    <button class="btn">Delete</button>
</form>
