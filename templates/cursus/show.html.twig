{% extends 'base.html.twig' %}

{% block title %}Cursus{% endblock %}

{% block body %}
    <h1>Cursus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cursu.id }}</td>
            </tr>
            <tr>
                <th>CodeCursus</th>
                <td>{{ cursu.codeCursus }}</td>
            </tr>
            <tr>
                <th>LibelleCursus</th>
                <td>{{ cursu.libelleCursus }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cursus_index') }}">back to list</a>

    <a href="{{ path('app_cursus_edit', {'id': cursu.id}) }}">edit</a>

    {{ include('cursus/_delete_form.html.twig') }}
{% endblock %}
