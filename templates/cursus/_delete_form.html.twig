<form method="post" action="{{ path('app_cursus_delete', {'id': cursus.id}) }}" onsubmit="return confirm('Êtes-vous sûr.e de vouloir supprimer ce cursus ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cursus.id) }}">
    <button class="btn bg-red text-clear m-5 align-end">Supprimer</button>
</form>
