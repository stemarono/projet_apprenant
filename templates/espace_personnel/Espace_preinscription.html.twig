{% extends 'base.html.twig' %}

{% block title %}Votre Espace personnel{% endblock %}

{% block body %}
 <div id="overlay" class="overlay">
        <div id="popup" class="popup">
            <h3 class="text-center"> {{app.user.prenom}}, êtes-vous sûre de vouloir supprimer votre dossier?</h3>
            <div class="text-center">
            <button id="btnClose" class="btnClose ">confirmer</button>
            <a href="{{path('app_espace_personnel')}}">annuler</a>
            </div>
        </div>
    </div>

    <div class="bg-blueclear p-5 my-4">
        <h1>Modifiez vos informations personnelles</h1>
    </div>

<class="container-fluid ">
        <div class="bg-personnal bg-blueclear p-5 mt-5 ">
            <h4><span class="text-capitalize">espace</span> personnel</h4>
        </div>
            <div class="d-flex justify-content-between">
                <div class="">
                        <div class="border  border-color shadow p-3 bg-body rounded my-5 ms-5 ps-5 w-100">
                            <div class="d-flex my-5">
                            
                                <div class="mx-5">
                                    <h5>Mon dossier</h5>
                                </div>
                                {# <div class="mx-5">
                                    <h5>Etat :</h5>
                                </div> #}
                            </div>
                           
                            <div class="d-flex my-5">
                                <a href="{{path('app_espace_personnel_showFolder')}}" class="mx-5 text-decoration-none btn btn-md btn-green ">Voir mon dossier</a>
                                <a href="{{path('app_espace_personnel_editFolder')}}" class="mx-5 text-decoration-none btn btn-md btn-blue ">Modifier</a>
                                <a href="javascript:supprimer()" class="mx-5 text-decoration-none btn btn-md btn-red">Supprimer</a>   
                            </div> 
                        </div>
                        <div class="d-flex justify-content-around mb-5">
                            <a class="text-decoration-none btn btn-md btn-green" href="{{path('app_formation_list')}}">Découvrir les formations</a>
                            <a class="text-decoration-none btn btn-md btn-green" href="{{path('app_pre_inscription_new')}}">Se pré-inscrire</a>
                            <a class ="text-decoration-none btn btn-md btn-blue" href="{{path('app_espace_personnel')}}">Revenir à la page précédente</a>
                        </div>
                </div>
                <div class=" border-2 border-start border-success my-5"></div>
                    <div class="me-5">
                    
                        <div class="my-5">
                            <h4>Informations personnelles</h4>
                        </div>
                        <div class="ms-5">
                            <p>Nom : {{app.user.nom}}</p>
                        </div>
                        <div class="ms-5">
                            <p>Prénom : {{app.user.prenom}}</p>
                        </div>
                        <div class="ms-5">
                            <p>Email : {{app.user.email}}</p>
                        </div>
                        <div class="ms-5">
                            <p>Mot de passe : ******</p>
                        </div>
                        <div>
                            <a class="btn btn-md btn-green text-light mt-5 ms-5" href="{{ path('app_user_editFolder', {'id': app.user.id}) }}">Modifier</a>
                        </div>
                   
                        <div class="">
                            <a href="{{ path('app_contact')}}" class="btn btn-md btn-green text-light my-5 ms-5">Nous contacter</a>
                        </div>
                </div>
            </div>
</div>
<script> 

// var url_delete_folder= "{{path('app_espace_personnel_deleteFolder')}}"
var url_delete="{{path('app_espace_personnel_deleteFolder')}}";

$(function(){
  
})
function supprimer()
{
    if(confirm ('{{app.user.prenom}}, Êtes-vous sûr(e) de vouloir supprimer votre dossier de pré-inscription ? ')){
       
       $.ajax({
        type:"POST",
        url: url_delete,
        data:{
          
        },
        beforeSend: function(){

        },

        success:function(response){
            alert(response);
        },
        error: function(){
            alert('il existe une erreur');
        },
       })
    }
}
</script>
{% endblock %}
