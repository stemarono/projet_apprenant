<form method="post" action="{{ path('app_apprenant_delete', {'id': apprenant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ apprenant.id) }}">
    <button class="btn">Delete</button>
</form>
